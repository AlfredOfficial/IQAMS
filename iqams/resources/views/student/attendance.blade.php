<x-student-layout title="My Attendance">
    <div class="mb-6"><h2 class="font-display text-2xl font-semibold text-slate-900">Attendance records</h2><p class="mt-1 text-sm text-slate-500">Your official attendance history. These records can only be changed by Admin.</p></div>
    <div class="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200/70"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th class="px-6 py-4">Date & time</th><th class="px-6 py-4">Subject</th><th class="px-6 py-4">Type</th><th class="px-6 py-4">Status</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($logs as $log)<tr><td class="whitespace-nowrap px-6 py-4">{{ \Illuminate\Support\Carbon::parse($log->scan_time)->format('M d, Y · g:i A') }}</td><td class="px-6 py-4">{{ $log->schedule?->subject?->subject_name ?? '—' }}</td><td class="px-6 py-4 capitalize">{{ str_replace('_', ' ', $log->attendance_type) }}</td><td class="px-6 py-4"><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold capitalize">{{ $log->status }}</span></td></tr>@empty<tr><td colspan="4" class="px-6 py-14 text-center text-slate-400">No attendance records yet.</td></tr>@endforelse</tbody></table></div>@if($logs->hasPages())<div class="border-t border-slate-100 p-4">{{ $logs->links() }}</div>@endif</div>
</x-student-layout>
