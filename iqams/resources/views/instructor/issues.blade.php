<x-instructor-layout title="Attendance Issues">
<div class="mb-6"><h2 class="text-xl font-semibold">Attendance Issues</h2><p class="text-sm text-slate-500">Automatically detected issues for the current month.</p></div>
<div class="space-y-4">@forelse($days as $day)<div class="flex flex-col justify-between gap-3 rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200/70 md:flex-row"><div><p class="font-semibold">{{ $day['date']->format('F j, Y') }}</p><p class="text-sm text-slate-500">@if($day['status']==='Absent')No Time-In recorded (Absent)@elseif($day['isIncomplete'])Missing: {{ $day['events']->filter(fn($v)=>!$v)->keys()->map(fn($v)=>str($v)->replace('_',' ')->title())->join(', ') }}@else{{ $day['notes']->join(', ') }}@endif</p></div><span class="self-start rounded-full bg-amber-50 px-3 py-1 text-sm text-amber-700">{{ $day['isIncomplete'] ? 'Incomplete' : $day['status'] }} · {{ $day['punctuality'] }}</span></div>@empty<div class="rounded-2xl bg-white p-8 text-center text-slate-500 shadow-sm">No attendance issues found this month.</div>@endforelse</div>
</x-instructor-layout>
